// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/DouDOU-start/airgate-core/ent/teamauditlog"
)

// TeamAuditLog is the model entity for the TeamAuditLog schema.
type TeamAuditLog struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 所属企业主 user id 快照。
	OwnerID int `json:"owner_id,omitempty"`
	// 操作者 user id：企业主本人，或成员账号（自建/改自己的密钥）。
	ActorUserID int `json:"actor_user_id,omitempty"`
	// ActorEmail holds the value of the "actor_email" field.
	ActorEmail string `json:"actor_email,omitempty"`
	// 动作：department.create / member.update / apikey.delete / team.billing_period …
	Action string `json:"action,omitempty"`
	// 对象类型：department / member / apikey / team
	TargetType string `json:"target_type,omitempty"`
	// TargetID holds the value of the "target_id" field.
	TargetID int `json:"target_id,omitempty"`
	// 对象名称快照，对象删除后仍可读。
	TargetName string `json:"target_name,omitempty"`
	// 变更前的关键字段快照；创建类动作为空。
	Before map[string]interface{} `json:"before,omitempty"`
	// 变更后的关键字段快照；删除类动作为空。
	After map[string]interface{} `json:"after,omitempty"`
	// IP holds the value of the "ip" field.
	IP string `json:"ip,omitempty"`
	// RequestID holds the value of the "request_id" field.
	RequestID string `json:"request_id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt    time.Time `json:"created_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TeamAuditLog) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case teamauditlog.FieldBefore, teamauditlog.FieldAfter:
			values[i] = new([]byte)
		case teamauditlog.FieldID, teamauditlog.FieldOwnerID, teamauditlog.FieldActorUserID, teamauditlog.FieldTargetID:
			values[i] = new(sql.NullInt64)
		case teamauditlog.FieldActorEmail, teamauditlog.FieldAction, teamauditlog.FieldTargetType, teamauditlog.FieldTargetName, teamauditlog.FieldIP, teamauditlog.FieldRequestID:
			values[i] = new(sql.NullString)
		case teamauditlog.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TeamAuditLog fields.
func (tal *TeamAuditLog) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case teamauditlog.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			tal.ID = int(value.Int64)
		case teamauditlog.FieldOwnerID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field owner_id", values[i])
			} else if value.Valid {
				tal.OwnerID = int(value.Int64)
			}
		case teamauditlog.FieldActorUserID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field actor_user_id", values[i])
			} else if value.Valid {
				tal.ActorUserID = int(value.Int64)
			}
		case teamauditlog.FieldActorEmail:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field actor_email", values[i])
			} else if value.Valid {
				tal.ActorEmail = value.String
			}
		case teamauditlog.FieldAction:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field action", values[i])
			} else if value.Valid {
				tal.Action = value.String
			}
		case teamauditlog.FieldTargetType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field target_type", values[i])
			} else if value.Valid {
				tal.TargetType = value.String
			}
		case teamauditlog.FieldTargetID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field target_id", values[i])
			} else if value.Valid {
				tal.TargetID = int(value.Int64)
			}
		case teamauditlog.FieldTargetName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field target_name", values[i])
			} else if value.Valid {
				tal.TargetName = value.String
			}
		case teamauditlog.FieldBefore:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field before", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &tal.Before); err != nil {
					return fmt.Errorf("unmarshal field before: %w", err)
				}
			}
		case teamauditlog.FieldAfter:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field after", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &tal.After); err != nil {
					return fmt.Errorf("unmarshal field after: %w", err)
				}
			}
		case teamauditlog.FieldIP:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field ip", values[i])
			} else if value.Valid {
				tal.IP = value.String
			}
		case teamauditlog.FieldRequestID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field request_id", values[i])
			} else if value.Valid {
				tal.RequestID = value.String
			}
		case teamauditlog.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				tal.CreatedAt = value.Time
			}
		default:
			tal.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TeamAuditLog.
// This includes values selected through modifiers, order, etc.
func (tal *TeamAuditLog) Value(name string) (ent.Value, error) {
	return tal.selectValues.Get(name)
}

// Update returns a builder for updating this TeamAuditLog.
// Note that you need to call TeamAuditLog.Unwrap() before calling this method if this TeamAuditLog
// was returned from a transaction, and the transaction was committed or rolled back.
func (tal *TeamAuditLog) Update() *TeamAuditLogUpdateOne {
	return NewTeamAuditLogClient(tal.config).UpdateOne(tal)
}

// Unwrap unwraps the TeamAuditLog entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (tal *TeamAuditLog) Unwrap() *TeamAuditLog {
	_tx, ok := tal.config.driver.(*txDriver)
	if !ok {
		panic("ent: TeamAuditLog is not a transactional entity")
	}
	tal.config.driver = _tx.drv
	return tal
}

// String implements the fmt.Stringer.
func (tal *TeamAuditLog) String() string {
	var builder strings.Builder
	builder.WriteString("TeamAuditLog(")
	builder.WriteString(fmt.Sprintf("id=%v, ", tal.ID))
	builder.WriteString("owner_id=")
	builder.WriteString(fmt.Sprintf("%v", tal.OwnerID))
	builder.WriteString(", ")
	builder.WriteString("actor_user_id=")
	builder.WriteString(fmt.Sprintf("%v", tal.ActorUserID))
	builder.WriteString(", ")
	builder.WriteString("actor_email=")
	builder.WriteString(tal.ActorEmail)
	builder.WriteString(", ")
	builder.WriteString("action=")
	builder.WriteString(tal.Action)
	builder.WriteString(", ")
	builder.WriteString("target_type=")
	builder.WriteString(tal.TargetType)
	builder.WriteString(", ")
	builder.WriteString("target_id=")
	builder.WriteString(fmt.Sprintf("%v", tal.TargetID))
	builder.WriteString(", ")
	builder.WriteString("target_name=")
	builder.WriteString(tal.TargetName)
	builder.WriteString(", ")
	builder.WriteString("before=")
	builder.WriteString(fmt.Sprintf("%v", tal.Before))
	builder.WriteString(", ")
	builder.WriteString("after=")
	builder.WriteString(fmt.Sprintf("%v", tal.After))
	builder.WriteString(", ")
	builder.WriteString("ip=")
	builder.WriteString(tal.IP)
	builder.WriteString(", ")
	builder.WriteString("request_id=")
	builder.WriteString(tal.RequestID)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(tal.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// TeamAuditLogs is a parsable slice of TeamAuditLog.
type TeamAuditLogs []*TeamAuditLog
