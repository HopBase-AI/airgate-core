// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/DouDOU-start/airgate-core/ent/predicate"
	"github.com/DouDOU-start/airgate-core/ent/subscriptionreservation"
	"github.com/DouDOU-start/airgate-core/ent/usersubscription"
)

// SubscriptionReservationQuery is the builder for querying SubscriptionReservation entities.
type SubscriptionReservationQuery struct {
	config
	ctx              *QueryContext
	order            []subscriptionreservation.OrderOption
	inters           []Interceptor
	predicates       []predicate.SubscriptionReservation
	withSubscription *UserSubscriptionQuery
	withFKs          bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SubscriptionReservationQuery builder.
func (srq *SubscriptionReservationQuery) Where(ps ...predicate.SubscriptionReservation) *SubscriptionReservationQuery {
	srq.predicates = append(srq.predicates, ps...)
	return srq
}

// Limit the number of records to be returned by this query.
func (srq *SubscriptionReservationQuery) Limit(limit int) *SubscriptionReservationQuery {
	srq.ctx.Limit = &limit
	return srq
}

// Offset to start from.
func (srq *SubscriptionReservationQuery) Offset(offset int) *SubscriptionReservationQuery {
	srq.ctx.Offset = &offset
	return srq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (srq *SubscriptionReservationQuery) Unique(unique bool) *SubscriptionReservationQuery {
	srq.ctx.Unique = &unique
	return srq
}

// Order specifies how the records should be ordered.
func (srq *SubscriptionReservationQuery) Order(o ...subscriptionreservation.OrderOption) *SubscriptionReservationQuery {
	srq.order = append(srq.order, o...)
	return srq
}

// QuerySubscription chains the current query on the "subscription" edge.
func (srq *SubscriptionReservationQuery) QuerySubscription() *UserSubscriptionQuery {
	query := (&UserSubscriptionClient{config: srq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := srq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := srq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(subscriptionreservation.Table, subscriptionreservation.FieldID, selector),
			sqlgraph.To(usersubscription.Table, usersubscription.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, subscriptionreservation.SubscriptionTable, subscriptionreservation.SubscriptionColumn),
		)
		fromU = sqlgraph.SetNeighbors(srq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first SubscriptionReservation entity from the query.
// Returns a *NotFoundError when no SubscriptionReservation was found.
func (srq *SubscriptionReservationQuery) First(ctx context.Context) (*SubscriptionReservation, error) {
	nodes, err := srq.Limit(1).All(setContextOp(ctx, srq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{subscriptionreservation.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (srq *SubscriptionReservationQuery) FirstX(ctx context.Context) *SubscriptionReservation {
	node, err := srq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SubscriptionReservation ID from the query.
// Returns a *NotFoundError when no SubscriptionReservation ID was found.
func (srq *SubscriptionReservationQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = srq.Limit(1).IDs(setContextOp(ctx, srq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{subscriptionreservation.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (srq *SubscriptionReservationQuery) FirstIDX(ctx context.Context) int {
	id, err := srq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SubscriptionReservation entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SubscriptionReservation entity is found.
// Returns a *NotFoundError when no SubscriptionReservation entities are found.
func (srq *SubscriptionReservationQuery) Only(ctx context.Context) (*SubscriptionReservation, error) {
	nodes, err := srq.Limit(2).All(setContextOp(ctx, srq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{subscriptionreservation.Label}
	default:
		return nil, &NotSingularError{subscriptionreservation.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (srq *SubscriptionReservationQuery) OnlyX(ctx context.Context) *SubscriptionReservation {
	node, err := srq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SubscriptionReservation ID in the query.
// Returns a *NotSingularError when more than one SubscriptionReservation ID is found.
// Returns a *NotFoundError when no entities are found.
func (srq *SubscriptionReservationQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = srq.Limit(2).IDs(setContextOp(ctx, srq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{subscriptionreservation.Label}
	default:
		err = &NotSingularError{subscriptionreservation.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (srq *SubscriptionReservationQuery) OnlyIDX(ctx context.Context) int {
	id, err := srq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SubscriptionReservations.
func (srq *SubscriptionReservationQuery) All(ctx context.Context) ([]*SubscriptionReservation, error) {
	ctx = setContextOp(ctx, srq.ctx, "All")
	if err := srq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SubscriptionReservation, *SubscriptionReservationQuery]()
	return withInterceptors[[]*SubscriptionReservation](ctx, srq, qr, srq.inters)
}

// AllX is like All, but panics if an error occurs.
func (srq *SubscriptionReservationQuery) AllX(ctx context.Context) []*SubscriptionReservation {
	nodes, err := srq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SubscriptionReservation IDs.
func (srq *SubscriptionReservationQuery) IDs(ctx context.Context) (ids []int, err error) {
	if srq.ctx.Unique == nil && srq.path != nil {
		srq.Unique(true)
	}
	ctx = setContextOp(ctx, srq.ctx, "IDs")
	if err = srq.Select(subscriptionreservation.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (srq *SubscriptionReservationQuery) IDsX(ctx context.Context) []int {
	ids, err := srq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (srq *SubscriptionReservationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, srq.ctx, "Count")
	if err := srq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, srq, querierCount[*SubscriptionReservationQuery](), srq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (srq *SubscriptionReservationQuery) CountX(ctx context.Context) int {
	count, err := srq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (srq *SubscriptionReservationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, srq.ctx, "Exist")
	switch _, err := srq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (srq *SubscriptionReservationQuery) ExistX(ctx context.Context) bool {
	exist, err := srq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SubscriptionReservationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (srq *SubscriptionReservationQuery) Clone() *SubscriptionReservationQuery {
	if srq == nil {
		return nil
	}
	return &SubscriptionReservationQuery{
		config:           srq.config,
		ctx:              srq.ctx.Clone(),
		order:            append([]subscriptionreservation.OrderOption{}, srq.order...),
		inters:           append([]Interceptor{}, srq.inters...),
		predicates:       append([]predicate.SubscriptionReservation{}, srq.predicates...),
		withSubscription: srq.withSubscription.Clone(),
		// clone intermediate query.
		sql:  srq.sql.Clone(),
		path: srq.path,
	}
}

// WithSubscription tells the query-builder to eager-load the nodes that are connected to
// the "subscription" edge. The optional arguments are used to configure the query builder of the edge.
func (srq *SubscriptionReservationQuery) WithSubscription(opts ...func(*UserSubscriptionQuery)) *SubscriptionReservationQuery {
	query := (&UserSubscriptionClient{config: srq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	srq.withSubscription = query
	return srq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ReservationKey string `json:"reservation_key,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SubscriptionReservation.Query().
//		GroupBy(subscriptionreservation.FieldReservationKey).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (srq *SubscriptionReservationQuery) GroupBy(field string, fields ...string) *SubscriptionReservationGroupBy {
	srq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SubscriptionReservationGroupBy{build: srq}
	grbuild.flds = &srq.ctx.Fields
	grbuild.label = subscriptionreservation.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ReservationKey string `json:"reservation_key,omitempty"`
//	}
//
//	client.SubscriptionReservation.Query().
//		Select(subscriptionreservation.FieldReservationKey).
//		Scan(ctx, &v)
func (srq *SubscriptionReservationQuery) Select(fields ...string) *SubscriptionReservationSelect {
	srq.ctx.Fields = append(srq.ctx.Fields, fields...)
	sbuild := &SubscriptionReservationSelect{SubscriptionReservationQuery: srq}
	sbuild.label = subscriptionreservation.Label
	sbuild.flds, sbuild.scan = &srq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SubscriptionReservationSelect configured with the given aggregations.
func (srq *SubscriptionReservationQuery) Aggregate(fns ...AggregateFunc) *SubscriptionReservationSelect {
	return srq.Select().Aggregate(fns...)
}

func (srq *SubscriptionReservationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range srq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, srq); err != nil {
				return err
			}
		}
	}
	for _, f := range srq.ctx.Fields {
		if !subscriptionreservation.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if srq.path != nil {
		prev, err := srq.path(ctx)
		if err != nil {
			return err
		}
		srq.sql = prev
	}
	return nil
}

func (srq *SubscriptionReservationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SubscriptionReservation, error) {
	var (
		nodes       = []*SubscriptionReservation{}
		withFKs     = srq.withFKs
		_spec       = srq.querySpec()
		loadedTypes = [1]bool{
			srq.withSubscription != nil,
		}
	)
	if srq.withSubscription != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, subscriptionreservation.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SubscriptionReservation).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SubscriptionReservation{config: srq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, srq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := srq.withSubscription; query != nil {
		if err := srq.loadSubscription(ctx, query, nodes, nil,
			func(n *SubscriptionReservation, e *UserSubscription) { n.Edges.Subscription = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (srq *SubscriptionReservationQuery) loadSubscription(ctx context.Context, query *UserSubscriptionQuery, nodes []*SubscriptionReservation, init func(*SubscriptionReservation), assign func(*SubscriptionReservation, *UserSubscription)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*SubscriptionReservation)
	for i := range nodes {
		if nodes[i].user_subscription_reservations == nil {
			continue
		}
		fk := *nodes[i].user_subscription_reservations
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(usersubscription.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_subscription_reservations" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (srq *SubscriptionReservationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := srq.querySpec()
	_spec.Node.Columns = srq.ctx.Fields
	if len(srq.ctx.Fields) > 0 {
		_spec.Unique = srq.ctx.Unique != nil && *srq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, srq.driver, _spec)
}

func (srq *SubscriptionReservationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(subscriptionreservation.Table, subscriptionreservation.Columns, sqlgraph.NewFieldSpec(subscriptionreservation.FieldID, field.TypeInt))
	_spec.From = srq.sql
	if unique := srq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if srq.path != nil {
		_spec.Unique = true
	}
	if fields := srq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, subscriptionreservation.FieldID)
		for i := range fields {
			if fields[i] != subscriptionreservation.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := srq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := srq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := srq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := srq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (srq *SubscriptionReservationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(srq.driver.Dialect())
	t1 := builder.Table(subscriptionreservation.Table)
	columns := srq.ctx.Fields
	if len(columns) == 0 {
		columns = subscriptionreservation.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if srq.sql != nil {
		selector = srq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if srq.ctx.Unique != nil && *srq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range srq.predicates {
		p(selector)
	}
	for _, p := range srq.order {
		p(selector)
	}
	if offset := srq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := srq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SubscriptionReservationGroupBy is the group-by builder for SubscriptionReservation entities.
type SubscriptionReservationGroupBy struct {
	selector
	build *SubscriptionReservationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (srgb *SubscriptionReservationGroupBy) Aggregate(fns ...AggregateFunc) *SubscriptionReservationGroupBy {
	srgb.fns = append(srgb.fns, fns...)
	return srgb
}

// Scan applies the selector query and scans the result into the given value.
func (srgb *SubscriptionReservationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, srgb.build.ctx, "GroupBy")
	if err := srgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SubscriptionReservationQuery, *SubscriptionReservationGroupBy](ctx, srgb.build, srgb, srgb.build.inters, v)
}

func (srgb *SubscriptionReservationGroupBy) sqlScan(ctx context.Context, root *SubscriptionReservationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(srgb.fns))
	for _, fn := range srgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*srgb.flds)+len(srgb.fns))
		for _, f := range *srgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*srgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := srgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SubscriptionReservationSelect is the builder for selecting fields of SubscriptionReservation entities.
type SubscriptionReservationSelect struct {
	*SubscriptionReservationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (srs *SubscriptionReservationSelect) Aggregate(fns ...AggregateFunc) *SubscriptionReservationSelect {
	srs.fns = append(srs.fns, fns...)
	return srs
}

// Scan applies the selector query and scans the result into the given value.
func (srs *SubscriptionReservationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, srs.ctx, "Select")
	if err := srs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SubscriptionReservationQuery, *SubscriptionReservationSelect](ctx, srs.SubscriptionReservationQuery, srs, srs.inters, v)
}

func (srs *SubscriptionReservationSelect) sqlScan(ctx context.Context, root *SubscriptionReservationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(srs.fns))
	for _, fn := range srs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*srs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := srs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
