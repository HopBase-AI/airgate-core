// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/DouDOU-start/airgate-core/ent/member"
	"github.com/DouDOU-start/airgate-core/ent/user"
)

// Member is the model entity for the Member schema.
type Member struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// 联系邮箱，仅供主账号辨识成员；不参与登录。
	Email string `json:"email,omitempty"`
	// Note holds the value of the "note" field.
	Note string `json:"note,omitempty"`
	// 成员额度（USD 账面口径，与 api_keys.quota_usd 同源）。0 表示不限。
	QuotaUsd float64 `json:"quota_usd,omitempty"`
	// 额度周期：none=一次性总额；monthly=按月重置（以 period_anchor 逐月对齐、月末夹紧）。
	QuotaPeriod member.QuotaPeriod `json:"quota_period,omitempty"`
	// 月度换期锚点（创建时刻），换期日与它同日对齐。
	PeriodAnchor time.Time `json:"period_anchor,omitempty"`
	// 当前计量期起点。monthly 时惰性推进；none 时仅记录最近一次手动重置。
	PeriodStart time.Time `json:"period_start,omitempty"`
	// 本期起点时 used_quota 的快照。本期已用 = used_quota − period_used_base。
	PeriodUsedBase float64 `json:"period_used_base,omitempty"`
	// 累计账面已用：累加 billed_cost（与 api_keys.used_quota 同口径）。
	UsedQuota float64 `json:"used_quota,omitempty"`
	// 累计真实成本：累加 actual_cost，即主账号为该成员实际付出的余额。
	UsedQuotaActual float64 `json:"used_quota_actual,omitempty"`
	// Status holds the value of the "status" field.
	Status member.Status `json:"status,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the MemberQuery when eager-loading is set.
	Edges        MemberEdges `json:"edges"`
	user_members *int
	selectValues sql.SelectValues
}

// MemberEdges holds the relations/edges for other nodes in the graph.
type MemberEdges struct {
	// Owner holds the value of the owner edge.
	Owner *User `json:"owner,omitempty"`
	// APIKeys holds the value of the api_keys edge.
	APIKeys []*APIKey `json:"api_keys,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// OwnerOrErr returns the Owner value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e MemberEdges) OwnerOrErr() (*User, error) {
	if e.Owner != nil {
		return e.Owner, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "owner"}
}

// APIKeysOrErr returns the APIKeys value or an error if the edge
// was not loaded in eager-loading.
func (e MemberEdges) APIKeysOrErr() ([]*APIKey, error) {
	if e.loadedTypes[1] {
		return e.APIKeys, nil
	}
	return nil, &NotLoadedError{edge: "api_keys"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Member) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case member.FieldQuotaUsd, member.FieldPeriodUsedBase, member.FieldUsedQuota, member.FieldUsedQuotaActual:
			values[i] = new(sql.NullFloat64)
		case member.FieldID:
			values[i] = new(sql.NullInt64)
		case member.FieldName, member.FieldEmail, member.FieldNote, member.FieldQuotaPeriod, member.FieldStatus:
			values[i] = new(sql.NullString)
		case member.FieldPeriodAnchor, member.FieldPeriodStart, member.FieldCreatedAt, member.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case member.ForeignKeys[0]: // user_members
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Member fields.
func (m *Member) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case member.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			m.ID = int(value.Int64)
		case member.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				m.Name = value.String
			}
		case member.FieldEmail:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field email", values[i])
			} else if value.Valid {
				m.Email = value.String
			}
		case member.FieldNote:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field note", values[i])
			} else if value.Valid {
				m.Note = value.String
			}
		case member.FieldQuotaUsd:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field quota_usd", values[i])
			} else if value.Valid {
				m.QuotaUsd = value.Float64
			}
		case member.FieldQuotaPeriod:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field quota_period", values[i])
			} else if value.Valid {
				m.QuotaPeriod = member.QuotaPeriod(value.String)
			}
		case member.FieldPeriodAnchor:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field period_anchor", values[i])
			} else if value.Valid {
				m.PeriodAnchor = value.Time
			}
		case member.FieldPeriodStart:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field period_start", values[i])
			} else if value.Valid {
				m.PeriodStart = value.Time
			}
		case member.FieldPeriodUsedBase:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field period_used_base", values[i])
			} else if value.Valid {
				m.PeriodUsedBase = value.Float64
			}
		case member.FieldUsedQuota:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field used_quota", values[i])
			} else if value.Valid {
				m.UsedQuota = value.Float64
			}
		case member.FieldUsedQuotaActual:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field used_quota_actual", values[i])
			} else if value.Valid {
				m.UsedQuotaActual = value.Float64
			}
		case member.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				m.Status = member.Status(value.String)
			}
		case member.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				m.CreatedAt = value.Time
			}
		case member.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				m.UpdatedAt = value.Time
			}
		case member.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field user_members", value)
			} else if value.Valid {
				m.user_members = new(int)
				*m.user_members = int(value.Int64)
			}
		default:
			m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Member.
// This includes values selected through modifiers, order, etc.
func (m *Member) Value(name string) (ent.Value, error) {
	return m.selectValues.Get(name)
}

// QueryOwner queries the "owner" edge of the Member entity.
func (m *Member) QueryOwner() *UserQuery {
	return NewMemberClient(m.config).QueryOwner(m)
}

// QueryAPIKeys queries the "api_keys" edge of the Member entity.
func (m *Member) QueryAPIKeys() *APIKeyQuery {
	return NewMemberClient(m.config).QueryAPIKeys(m)
}

// Update returns a builder for updating this Member.
// Note that you need to call Member.Unwrap() before calling this method if this Member
// was returned from a transaction, and the transaction was committed or rolled back.
func (m *Member) Update() *MemberUpdateOne {
	return NewMemberClient(m.config).UpdateOne(m)
}

// Unwrap unwraps the Member entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (m *Member) Unwrap() *Member {
	_tx, ok := m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Member is not a transactional entity")
	}
	m.config.driver = _tx.drv
	return m
}

// String implements the fmt.Stringer.
func (m *Member) String() string {
	var builder strings.Builder
	builder.WriteString("Member(")
	builder.WriteString(fmt.Sprintf("id=%v, ", m.ID))
	builder.WriteString("name=")
	builder.WriteString(m.Name)
	builder.WriteString(", ")
	builder.WriteString("email=")
	builder.WriteString(m.Email)
	builder.WriteString(", ")
	builder.WriteString("note=")
	builder.WriteString(m.Note)
	builder.WriteString(", ")
	builder.WriteString("quota_usd=")
	builder.WriteString(fmt.Sprintf("%v", m.QuotaUsd))
	builder.WriteString(", ")
	builder.WriteString("quota_period=")
	builder.WriteString(fmt.Sprintf("%v", m.QuotaPeriod))
	builder.WriteString(", ")
	builder.WriteString("period_anchor=")
	builder.WriteString(m.PeriodAnchor.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("period_start=")
	builder.WriteString(m.PeriodStart.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("period_used_base=")
	builder.WriteString(fmt.Sprintf("%v", m.PeriodUsedBase))
	builder.WriteString(", ")
	builder.WriteString("used_quota=")
	builder.WriteString(fmt.Sprintf("%v", m.UsedQuota))
	builder.WriteString(", ")
	builder.WriteString("used_quota_actual=")
	builder.WriteString(fmt.Sprintf("%v", m.UsedQuotaActual))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", m.Status))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Members is a parsable slice of Member.
type Members []*Member
